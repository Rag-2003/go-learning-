package main

import (
	"fmt"
)

func main() {
	// numbers := [] int32{1,2}
	// numbers = append(numbers, 6, 7, 8, 9, 10)
	// fmt.Println("number has data type", numbers)
	// fmt.Printf("How to check  the type %T\n", numbers)
	// fmt.Println("length:", len(numbers))
	// fmt.Println(("make iot more attractivre more then wizulazile"))
	// numbers := make([]float32, 3, 5)
	// numbers = append(numbers, 4)
// 	numbers = append(numbers, 43)
// 	numbers = append(numbers, 45)
// 	numbers = append(numbers, 4)
// 	numbers = append(numbers, 43)
// 	numbers = append(numbers, 45)

// 	fmt.Println("slice", numbers)
// 	fmt.Println("length", len(numbers))
// 	fmt.Println("capacity", cap(numbers))
rag()
 }

func rag() {
	 number1 := make([]string, 0)
	fmt.Println("addition  nbumbr we can  ad over the string", number1)
}
